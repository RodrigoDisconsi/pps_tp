import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: '7-VisualizadorKinetico',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
